# Exclude specific files and directories from redirect
!/css/*    # Exclude the /css directory from redirect
!/favicon.svg    # Exclude the favicon.ico file from redirect
!/android-chrome-192x192.png
!/android-chrome-512x512.png
!/apple-touch-icon.png
!/site.webmanifest
!/og-images/*

# Redirect any URLs with the default language code in them already
/en/(.*)    /$1    301!
/en/(.*)    /404/index.html    404

# Important: Per shadowing rules, URLs for the
# _non-default_ language-specific content files
# are served without redirects.

# Default
/(.*)    /en/$1    302